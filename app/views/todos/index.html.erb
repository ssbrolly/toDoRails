<h1> Todo list </h1>
<p><%= link_to 'New Todo', new_todo_path%></p>
<% @todos.each do |todo| %>
  <h2><%= todo.name%><h2>
  <%= link_to 'Show Todos', todo_path(todo)%>
  <p><%= link_to 'New Todo', new_todo_path %></p>

  <% end %>
